# Database Configuration (Docker PostgreSQL)
# 
# IMPORTANT: When running the app in Docker Compose, use DB_HOST=postgres
# When running the app locally (outside Docker), use DB_HOST=localhost
# 
# For Docker Compose (full stack):
#   DB_HOST=postgres
# 
# For local development (only database in Docker):
#   DB_HOST=localhost
#
DB_HOST=postgres
DB_PORT=5432
DB_NAME=search_engine
DB_USER=search_user
DB_PASSWORD=search_password_2026

# Application Settings
APP_ENV=development
APP_DEBUG=true
APP_TIMEZONE=UTC

# Search Configuration
RESULTS_PER_PAGE=10
DEFAULT_SEARCH_MODE=and
MAX_QUERY_LENGTH=200

# Performance
ENABLE_QUERY_CACHE=false
CACHE_TTL=3600
